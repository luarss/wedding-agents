[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wedding-agent"
version = "0.1.0"
description = "AI-powered Singapore wedding venue comparison"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "crewai>=1.4.1",
    "python-dotenv>=1.2.1",
]

[tool.hatch.build.targets.wheel]
packages = ["backend"]
